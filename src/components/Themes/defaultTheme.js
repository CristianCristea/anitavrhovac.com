import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#08a045'
    },
    white: '#fff',
    textPrimary: '#C4C7CA'
  },
  typography: {}
});

export default theme;
